            [project]
            name = "ephyorm"
            version = "0.1.0"
            description = "A Python project generated by Python Initializr"
            readme = "README.md"
            requires-python = ">=3.12"
            license = {text = "MIT"}
            dependencies = [
            "psycopg2-binary>=2.9.12",
]

            [project.scripts]
            ephyorm = "ephyorm:main"

            [build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

